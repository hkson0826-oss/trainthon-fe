# 신고 지도

- `/map`: 로그인한 모든 사용자의 신고 지도. 지도 영역을 이동하면 서버에서 해당 범위의 신고를 조회한다.
- `/incidents/:id`: 로그인한 모든 사용자의 신고 상세와 위치. 신고자 관리와 영상·정산은 기존 별도 권한을 사용한다.
- 신고 등록 화면에서 지도 클릭 또는 현재 위치 버튼으로 사고 지점을 선택하고 장소명·주소를 입력한다. 현재 위치 권한을 거절해도 지도 선택으로 등록할 수 있다.
- 좌표는 신고에 저장되므로 장소 정보가 나중에 바뀌어도 신고 시점의 위치가 유지된다.
- 지도는 Leaflet과 OpenStreetMap을 사용한다. 별도 API 키는 필요 없다. 타일 제공자를 바꾸려면 `NEXT_PUBLIC_MAP_TILE_URL`과 컴포넌트의 출처 표기를 함께 변경한다.
- 운영 환경은 `NEXT_PUBLIC_USE_MOCK_API=false`, 실제 API 주소, Supabase URL·공개 anon key, 기존 데모 로그인 설정이 필요하다. API 서비스 역할 키는 프론트에 넣지 않는다.
- 백엔드 `0006_incident_locations.sql` 마이그레이션과 지도 API를 먼저 배포한다. 백엔드 시작 시 미적용 마이그레이션이 실행된다.

검증: `npm test`, `npm run typecheck`, `npm run build`. 프론트 테스트는 `src/`만 실행하며 백엔드 테스트는 별도 저장소에서 실행한다.
