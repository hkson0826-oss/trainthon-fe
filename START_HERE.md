# Lumina 원샷 개발 문서 사용법

이 폴더는 아이디어와 개발 지시 명세를 담는다. 앱 소스코드나 실제 결제 연동이 구현된 상태는 아니다.

## 문서 구성

1. [PRODUCT_CONTRACT.md](./PRODUCT_CONTRACT.md): 사업 구조, MVP 가정, 공통 API·데이터·상태·금전 불변식. 두 에이전트가 함께 읽는다.
2. [BE_MASTER.md](./BE_MASTER.md): 백엔드 개발 에이전트용 상세 지시. 서버·DB·AI·결제·반환·정산·테스트를 담당한다.
3. [FE_MASTER.md](./FE_MASTER.md): 프론트엔드 개발 에이전트용 상세 지시. 메신저형 등록·결제·매칭·수령·보상 화면과 API 통합을 담당한다.

각 MASTER는 본인 역할의 최상위 작업 명세다. 공통 계약과 함께 전달한다. 별도 작업 공간에서 개발하더라도 동일한 PRODUCT_CONTRACT 버전을 사용해야 한다.

## 확정된 사업 방향

분실자가 탐색 서비스를 구매한다. 습득물 등록은 무료다. 반환 확인 뒤 결제금 일부를 습득자에게 지급한다. 이번 MVP는 5,000원 결제→3,500원 습득자 보상+1,500원 플랫폼 배분으로 고정한다.

7일 탐색, 미반환 시 전액 환불, 웹 우선, 단일 캠퍼스는 원샷 구현에 필요한 미정사항을 채운 가정이다. 실제 카카오톡 채널, 특정 PG 계약, 학교 신원 인증이 이미 준비되어 있다고 가정하지 않는다. 변경하려면 구현 전에 PRODUCT_CONTRACT와 두 MASTER에 같은 결정을 반영한다.

## 두 에이전트에 각각 전달할 프롬프트

### BE 에이전트

```text
PRODUCT_CONTRACT.md와 BE_MASTER.md를 전부 읽고 Lumina 백엔드를 구현하라.
계획 제시에 그치지 말고 명세의 로컬 실행·검증·통합 완료 기준까지 수행하라.
원본 https://github.com/Icey067/Lumina.git 구조를 참고하되 현재 작업 공간부터 확인하라.
기존 사용자 파일과 변경 사항을 보존하고, 계약의 server/ 및 shared/openapi.yaml 소유권을 지켜라.
FE가 병행 개발하므로 OpenAPI와 공통 fixture shape부터 전달하고 계약을 임의로 바꾸지 마라.
외부 키가 없으면 명시된 demo adapter로 완결된 로컬 동작을 만들고 live 지원 상태를 정확히 보고하라.
실제 인증 없는 송금이나 브라우저 성공값에 의존하는 결제 처리를 만들지 마라.
완료 시 변경 파일, 실행 방법, 테스트 결과, 남은 live 연동 조건을 보고하라.
```

### FE 에이전트

```text
PRODUCT_CONTRACT.md와 FE_MASTER.md를 전부 읽고 Lumina 프론트엔드를 구현하라.
계획 제시에 그치지 말고 명세의 모든 핵심 화면과 로컬 통합 검증까지 수행하라.
원본 https://github.com/Icey067/Lumina.git 구조를 참고하되 현재 작업 공간부터 확인하라.
기존 사용자 파일과 변경 사항을 보존하고, 계약의 FE 파일 소유권을 지켜라.
BE가 병행 개발하므로 동일 OpenAPI와 fixture shape를 소비하고 서버 정책을 UI에서 별도로 결정하지 마라.
계약 mock으로 병행 개발 후 실제 API 모드에서 등록→결제→매칭→승인→반환→보상 흐름을 검증하라.
한국어 모바일 우선 UX, 새로고침 복원, 오류 복구, 접근성, demo 표기를 완성하라.
완료 시 변경 파일, 실행 방법, 테스트 결과, 남은 live 연동 조건을 보고하라.
```

## 실행 환경 준비 원칙

- 현재 이 폴더에는 참조 저장소 checkout이 없다. 구현 에이전트는 사용자 코드 유무를 먼저 확인하고 별도 Lumina 작업 폴더를 만들거나 지정된 checkout에서 작업한다.
- 문서는 참조 저장소의 root FE + server/ 구조를 유지하는 신규 설계다. Firebase 직접 도메인 저장과 Express SQLite를 혼용하는 원본 상태를 그대로 복제하지 않는다.
- BE는 server/package.json, FE는 root package.json을 소유한다. 둘이 root lockfile을 동시에 수정하지 않는다.
- 외부 서비스 비밀 키는 서버 환경 설정으로만 전달하고 문서·소스·브라우저 번들에 포함하지 않는다.
- 원샷은 추가 기획 없이 구현할 수 있는 구체성을 뜻한다. 외부 계정 개설, 공급자 심사, 실제 운영 가능성이 자동 보장되는 의미는 아니다.

## 최종 검수

통합 데모 두 계정(분실자/습득자)을 사용해 정상 흐름과 실패 흐름을 확인한다. 핵심 실패 흐름은 결제 실패, AI 실패, 후보 없음, 소유권 거절, 이미 예약된 물건, 코드 만료, 검색 취소/만료, 환불 지연, 지급 지연이다. 새로고침이나 서버 재시작으로 결제·반환·정산이 중복되면 완료로 판정하지 않는다.

